# 2high2try test bot config
# Copy this file to bot-config.yml (same folder) and fill in your values.
# bot-config.yml is gitignored — the token never gets committed.

# Bot token from https://discord.com/developers/applications -> your app -> Bot -> Reset Token
token: "PASTE_TOKEN_HERE"

# Right-click your Discord server icon -> Copy Server ID (enable Developer Mode in Discord settings first)
guild_id: 0

# Right-click the testing channel -> Copy Channel ID
channel_id: 0
